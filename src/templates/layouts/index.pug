doctype html

block import

block vars

- const url = 'https://test.com';
- const siteName = 'ABOUT OUR COFFEE';
- const facebookAppId = 'test id';
- const facebookPageUrl = 'facebook url';
- const twitterCardType = 'twitter card type';
- const twitterAccount = '@';

html(lang=dic.lang)
  head
    meta(charset="utf-8")
    meta(http-equiv="X-UA-Compatible" content="IE=edge")
    meta(name="viewport" content="width=device-width, height=device-height, initial-scale=1, maximum-scale=5, minimum-scale=1, user-scalable=yes, viewport-fit=cover")
    link(rel="manifest" href="/manifest.json")
    link(rel="stylesheet" href="/styles/index.css")
    script
      include /layouts/service-worker.js
    block meta
  body
    block body
