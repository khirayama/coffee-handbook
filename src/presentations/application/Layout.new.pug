doctype html

html(lang=props.lang)
  head
    meta(charset="utf-8")
    meta(http-equiv="X-UA-Compatible" content="IE=edge")
    meta(name="viewport" content="width=device-width, height=device-height, initial-scale=1, maximum-scale=5, minimum-scale=1, user-scalable=yes, viewport-fit=cover")
    link(rel="manifest" href='/manifest.json')
    link(rel="stylesheet" href="/application/index.css"  media="screen")

    link(rel="alternate" hreflang="en" href=props.url.en)
    link(rel="alternate" hreflang="ja" href=props.url.ja)
    link(rel="alternate" hreflang="x-default" href=props.url.en)

    title= props.title
    meta(name="description" content=props.description)
    meta(name="author" content=props.author)
    meta(name="keywords" content=props.keywords.join(','))
    link(rel="contents" href="/sitemap.xml")
    link(href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet")
    link(rel="icon" href=`/images/icon_${props.lang}_circle.png` type="image/png")
    link(rel="preconnect" href="//www.mapbox.com" crossorigin)
    link(rel="preconnect" href="//www.openstreetmap.com" crossorigin)
    link(rel="preconnect" href="//events.mapbox.com" crossorigin)
    link(rel="preconnect" href="//www.google-analytics.com" crossorigin)

    meta(name="theme-color" content="white")

    //- OGP
    meta(property="og:site_name" content=props.name)
    meta(property="og:title" content=props.title)
    meta(property="og:description" content=props.description)
    meta(property="og:image" content=props.images.rectangle[props.lang])
    meta(property="og:type" content=props.pageType)
    meta(property="og:url" content=`${props.baseUrl}${props.path}`)
    //- Facebook
    meta(property="fb:app_id" content=props.facebookAppId)
    meta(property="article:publisher" content=props.facebookPageUrl)
    //- Twitter
    meta(property="twitter:title" content=props.title)
    meta(property="twitter:description" content=props.description)
    meta(property="twitter:image" content=props.images.square[props.lang])
    meta(property="twitter:card" content=props.twitterCardType)
    meta(property="twitter:site" content=props.twitterAccount)
    meta(property="twitter:url" content=`${props.baseUrl}${props.path}`)

    script(type="application/ld+json").
      !{JSON.stringify(props.jsonLD)}
    script.
      window.options = {
        env: '#{props.env}',
        lang: '#{props.lang}',
        gaCode: '#{props.gaCode}',
        route: '#{props.route}',
      };
      window.state = !{JSON.stringify(props.state)};
    script.
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');
    script(src="/vendors.js" defer)
    script(src="/commons.js" defer)
    script(src=props.entrypoint defer)
    link(rel="stylesheet" href=props.stylesheet media="screen")
    link(href="https://api.mapbox.com/mapbox-gl-js/v0.50.0/mapbox-gl.css" rel="stylesheet" media="screen")
    script
      include ../../public/application/bundle.js
  body
    .application
      != props.content
