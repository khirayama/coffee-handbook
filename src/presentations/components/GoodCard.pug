include /components/Picture

mixin GoodCard(good)
  a.GoodCard(href=good.url)
    .GoodCard--Picture
      +Picture(good.meta.thumbnailUrl.square, good.title)
    .GoodCard--Name= good.name
    .GoodCard--Category= good.category
